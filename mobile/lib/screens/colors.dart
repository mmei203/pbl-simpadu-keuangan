import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF2C4593);

  // Secondary
  static const Color secondary = Color(0xFFB8C7D9);

  // Background
  static const Color background = Color(0xFFE8EFED);

  // Surface / Card
  static const Color surface = Color(0xFFC4C9D1);

  // Main (Success / highlight)
  static const Color success = Color(0xFF2FA85E);

  // Warning / Error
  static const Color error = Color(0xFFE74C3C);

  // Button variants
  static const Color mainButton = Color(0xFF2F7AA3);
  static const Color editButton = Color(0xFFF39C12);
  static const Color backButton = Color(0xFF9FA4AA);

  // Text
  static const Color textPrimary = Colors.black;
  static const Color textOnPrimary = Colors.white;
}